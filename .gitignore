# No gitignore for bin binaries — they must be tracked (even as 0-byte placeholders)
# for //go:embed to work when Go module consumers download via go get.
# Release workflow replaces placeholders with real binaries and force-adds them.
